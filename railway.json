{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/web && npm install && cd ../.. && npx prisma generate --schema prisma/schema.prisma && cd apps/web && npm run build"
  },
  "deploy": {
    "startCommand": "npx prisma db push --schema prisma/schema.prisma --accept-data-loss && cd apps/web && npm start",
    "restartPolicyType": "ON_FAILURE"
  }
}
